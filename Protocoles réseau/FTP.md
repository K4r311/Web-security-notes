ftp( file transfer protocol) est un protocole de transfert de fichiers. Il est principalement utilisé pour upload et download des fichiers.
Pour se connecter a ftp, faire:
```bash
ftp "target_ip" 'port'
```
